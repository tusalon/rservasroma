name: Build Rservasroma Admin APK (TWA Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: TWA disabled
        run: |
          echo "Bubblewrap/TWA esta deshabilitado."
          echo "Usar .github/workflows/build-android-apk.yml para compilar con Capacitor."
