# DESHABILITADO — reemplazado por build-apk.yml (bubblewrap/TWA)
name: Build Android APK (DISABLED)

on:
  workflow_dispatch:

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "Este workflow está deshabilitado. Usar build-apk.yml"
